export const getMessageId = (formId: string, fieldName: string) => `${formId}-${fieldName}-message`;

export const getControlId = (formId: string, fieldName: string) => `${formId}-${fieldName}-control`;
